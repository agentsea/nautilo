{
  "name": "@nautilo/reset-pin-bin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Physical access PIN reset — bypasses server, operates directly on DB",
  "scripts": {
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "clean": "rm -rf dist .turbo node_modules"
  },
  "dependencies": {
    "@nautilo/db": "workspace:*",
    "@nautilo/trust": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.10.4",
    "typescript": "5.9.3"
  }
}
