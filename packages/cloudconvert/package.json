{
  "name": "@nautilo/cloudconvert",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CloudConvert SDK adapter for optional file conversion (D306)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "clean": "rm -rf dist .turbo node_modules",
    "test": "bun test --timeout 15000 src/",
    "test:unit": "bun test --timeout 15000 src/"
  },
  "dependencies": {
    "cloudconvert": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "24.10.4",
    "typescript": "5.9.3"
  }
}
