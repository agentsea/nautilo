import { BUNDLED_APPLICATION_CATALOGUE_V1, type ApplicationCatalogueV1 } from "@nautilo/types";
/** Offline, parsed, immutable fallback. Remote input never changes support IDs. */
export const bundledApplicationCatalogue: ApplicationCatalogueV1 = BUNDLED_APPLICATION_CATALOGUE_V1;
