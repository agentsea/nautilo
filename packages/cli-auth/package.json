{
  "name": "@nautilo/cli-auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared CLI auth primitives: loopback PKCE, device flow, headless detection, browser launcher (M108).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "clean": "rm -rf dist .turbo node_modules",
    "test": "bun test",
    "test:unit": "bun test tests/unit/"
  },
  "dependencies": {
    "@nautilo/api-client": "workspace:*",
    "@nautilo/logger": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.10.4",
    "typescript": "5.9.3"
  }
}
