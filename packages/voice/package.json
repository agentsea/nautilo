{
  "name": "@nautilo/voice",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Voice pipeline — TTS output and STT input for Nautilo",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "bun test --timeout 15000",
    "test:unit": "bun test --timeout 15000 tests/unit/"
  },
  "dependencies": {
    "@nautilo/config": "workspace:*",
    "@nautilo/logger": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.10.4",
    "typescript": "5.9.3"
  }
}
