-- D211: reverse D201 per-viewer presentation substrate.
-- The premise (shared Agent + per-viewer overrides) contradicted the
-- canonical entity model: each Human owns and customizes their own Agent.
DROP TABLE IF EXISTS user_agent_preferences;
