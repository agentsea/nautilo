import { Stack } from "expo-router";

// D369 Phase 1 onboarding group: add-server (Phase 1) + sign-in (Phase 2).
export default function OnboardingLayout() {
  return <Stack screenOptions={{ headerShown: true }} />;
}
