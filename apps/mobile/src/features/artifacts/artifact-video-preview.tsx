import { Text } from "react-native";
export function ArtifactVideoPreview(_props: { artifactId: string; revision: number }) {
  return <Text>In-app video playback requires the Nautilo iPhone or Android app. You can save the original to open it elsewhere.</Text>;
}
