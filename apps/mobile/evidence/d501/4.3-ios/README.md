# D501 Task 4.3 iOS live-acceptance evidence

Run `d501-ios-source-actions.yaml` only with a current iOS branch build on the
isolated Stack 302 clone and simulator/runtime. Supply a disposable Markdown
fixture's canonical API `id` as `D501_MARKDOWN_ID`. Never commit credentials,
tokens, fixture IDs, clone-private paths, or private content/screenshots.

The harness proves only route admission, source input, Markdown action
reachability, Preview/Edit continuity, and Save-action retention. Manual proof
still owns native selection handles, IME composition, Unicode paste, exact
Markdown/plain-text save-and-reopen bytes, dirty-back recovery, conflicts,
keyboard geometry, and large-source scrolling.

Canonical Writer artifacts are viewer-only on iOS and Android in D501. The
iOS matrix must prove readable native Writer rendering with no Edit affordance,
editor, toolbar, diminished-capability banner, or future-support promise.

Create `manifest.json` only after every row has direct redacted evidence and
the disposable fixtures are restored or removed. This scaffold is not live
evidence and does not make a completion claim.
