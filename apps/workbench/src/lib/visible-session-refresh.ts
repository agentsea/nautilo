/**
 * Shared recovery cadence for viewer-private state whose realtime hints are
 * best effort. Established by M236 and reused by M323 while connected/visible.
 */
export const VISIBLE_SESSION_REFRESH_INTERVAL_MS = 60_000;
